1º | Añadir ICONOS al Sidebar (muy visual) | OK

2º | Sidebar con highlight

3º | Añadir un Footer simple al layout

4º | Añadir una página de Login

    Esto sería un salto pequeño pero MUY útil:

    Formulario simple (email, contraseña)

    Componentes ShadCN (Input + Button + Card)

    Validación mínima

    Página separada del layout

    Preparado para conectar al backend NestJS en cuanto lo tengas

    El layout se vería así:

    /login → sin sidebar
    /home → con sidebar + header

5º | Añadir un sistema de “feature modules” real

    Cada uno con:

    pages

    components

    services

    hooks

    Para tener una arquitectura escalable desde ya.

6 — Añadir un “Notificador” (toast) con ShadCN

    opcional

    Ya tienes sonner instalado.
    Podemos añadir:

    import { Toaster } from "@/components/ui/sonner";

    Y desde cualquier sitio lanzar:

    toast("Guardado con éxito!");

    Esto será clave para el ERP.

7º | Añadir Tipografía profesional OK

8º | Modo oscuro automático (tienes next-themes instalado)

    por defecto light.
    y se ignora el system
    añadir en futuro metodo para cambiar de modo

    <ThemeProvider>
        <App />
    </ThemeProvider>

    Y usar:

    import { ModeToggle } from "@/components/mode-toggle";

9º | idiomas?

10º | Creamos un “PageContainer”

    para maquetar todas las páginas igual

11º | lazy loading

    Router con lazy loading usando React.lazy() + Suspense
